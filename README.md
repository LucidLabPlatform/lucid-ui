# lucid-ui

Dashboard and operator-facing web UI for LUCID.

Owns:
- login/session handling
- Jinja templates and static assets
- HTTP proxying to backend services
- WebSocket proxying to `lucid-orchestrator`
